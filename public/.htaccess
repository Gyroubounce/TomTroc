RewriteEngine On

# Si le fichier ou dossier demandé existe, on le sert
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Sinon, on redirige vers index.php
RewriteRule ^ index.php [QSA,L]
