<?php
/**
 * Fichier helpers.php
 *
 * Contient des exemples de fonctions utilitaires globales qui pourront être
 * ajoutées lorsque le projet dépassera le MVP. Ce fichier sert de point central
 * pour regrouper les petites fonctions réutilisables dans tout le projet.
 *
 * Exemples de helpers qui pourront être ajoutés plus tard :
 *
 * - redirect('/login')
 *     Redirige l'utilisateur vers une autre page.
 *
 * - dd($variable)
 *     Affiche proprement une variable pour le debug puis stoppe l'exécution.
 *
 * - sanitize($string)
 *     Nettoie une chaîne pour éviter les failles XSS.
 *
 * - asset('css/style.css')
 *     Génère une URL complète vers un asset public.
 *
 * - isLoggedIn()
 *     Vérifie si un utilisateur est connecté via la session.
 *
 * Ce fichier est chargé une seule fois dans index.php.
 * À développer davantage lorsque le projet dépassera le MVP.
 */
